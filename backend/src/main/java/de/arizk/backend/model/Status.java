package de.arizk.backend.model;


// note-to-self: enum, weil wir nur eine feste menge an werten brauchen, nicht wie bei klasse komplexe objekte mit vielen eigenschaften modellieren
// sicherer
public enum Status {
    OPEN,
    IN_PROGRESS,
    DONE
}
